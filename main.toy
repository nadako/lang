function main(cond:Bool) {
    var answer = 42;
    if (cond)
        monoFun(answer);
    else
        while ({trace("sup"); true;})
            trace(if ({trace("cond"); cond;}) "hi" else "bye");
    trace("YO");
}
